cosign.key
_build_*
output
_build-*/**

# The operator's filled-in machine configuration, and anything rendered from
# it. Both carry the Portainer password in plain text and this repository is
# public (REQUIREMENTS.md §req:quality-attributes).
kantainer.conf
*.ign
